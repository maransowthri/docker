# `docker network create` Commands

## To create a new docker network

```bash
docker network create <network_name>
```

## To create overlay network with encrypted communication

```bash
docker network create --driver overlay --opt encrypted mynetwork
```
